{% extends "AppLouvreBundle::layout.html.twig" %}


{% block body %}

  <h2>Etape 1</h2>
  
  {% for type, messages in app.session.flashbag.all %}
    {% for message in messages %}
      {%if type == 'error'%} {% set type = 'danger' %} {%endif%}
        <div class="alert alert-{{ type }}">
            {{ message|raw }}
        </div>
    {% endfor %}
  {% endfor %}

	 <div class="container">

    <div class="row">
      <div id="content" class="col-sm-12 col-md-8">
       		<div class="well">
       		
       			{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

       		
       				<fieldset>
						
						<div class="form-group">
							
							{{ form_errors(form.visitDate) }}
							{{ form_widget(form.visitDate, {'attr': {'class': 'datpicker'}}) }}
						</div>
						<br/>
						<div class="form-group">
							
								{{ form_widget(form.ticketsType) }}
						</div>
						<div class="form-group">
							
							{{ form_widget(form.nbTickets, {'attr': {'required': 'required'}}) }}
						</div>
							

					</fieldset>
		{{ form_widget(form._token) }}
		{{ form_rest(form) }}
   				
   				
   				{{ form_end(form) }}
   				
   				
  			</div>
      </div>
      <div id="right-widget" class="col-md-4 ticket-stats hidden-sm hidden-xs">
        <h2>Actuellement</h2>
        
      </div>
    </div>

    
  </div>
	
  

   <!-- Modal -->
  <div id="modal-price" class="modal fade" role="dialog">
    <div class="modal-dialog">

      <!-- Modal content-->
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
          <h4 class="modal-title">Conditions et justificatis tarif réduit :</h4>
        </div>
        <div class="modal-body">
            <p>Le billet tarif réduit est appliqué sous certaines conditions, à savoir les statuts suivant sont éligibles* à cette réduction : employé du musée, d’un service du Ministère de la Culture, militaire, étudiant, chercheur d'emploi...</p>
            <small>* Sous réserve de présenter le justificatif adéqute (carte d'étudiant, statut pôle emploi, etc...).</small>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Fermer</button>
        </div>
      </div>

    </div>
  </div>

  

{% endblock %}
